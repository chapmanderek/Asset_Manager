<%= form_for(@assignment_log) do |f| %>
  <% if @assignment_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assignment_log.errors.count, "error") %> prohibited this assignment_log from being saved:</h2>

      <ul>
      <% @assignment_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :staff %><br>
    <%= f.text_field :staff %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
